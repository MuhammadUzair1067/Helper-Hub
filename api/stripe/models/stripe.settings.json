{
  "kind": "collectionType",
  "collectionName": "stripes",
  "info": {
    "name": "Stripe"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "cleaner": {
      "via": "stripe",
      "model": "cleaner"
    },
    "customerID": {
      "type": "string"
    },
    "subscriptionStatus": {
      "type": "enumeration",
      "enum": [
        "trial",
        "non",
        "subscribed",
        "canceled"
      ]
    },
    "subscriptionID": {
      "type": "string"
    }
  }
}
